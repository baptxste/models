[project]
name = "models"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gliner<0.2.10",
    "onnxruntime<=1.18.1",
    "torch<=2.2.2",
    "transformers<4.40",
]
